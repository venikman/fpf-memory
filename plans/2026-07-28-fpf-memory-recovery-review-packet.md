# fpf-memory recovery — FPF project review packet (2026-07-28)

Route: `route:boundary-unpacking` — ask_fpf 2026-07-28, confidence 0.92, snapshot
`sha256:e2312049…` built 2026-07-28T08:47Z [fact]. Caveat [fact]: the hosted runtime
serves upstream `44dd8818` (2026-07-12), 16 days stale; the planning patterns applied
here (A.6, A.1.1, A.15, F.11, F.17) are assumed stable across that window [assumption].

## Context (A.1.1)

One bounded context: **fpf-memory delivery-pipeline recovery** — the repo
`venikman/fpf-memory`, its GitHub Actions, and its two production surfaces
fpf.sh + mcp.fpf.sh. Local vocabulary: **fresh** = `publication.upstreamRef`
equals ailev/FPF HEAD within the drift SLO; **outage** = scheduled sync run red;
**snapshot** = the derived ~95MB runtime index artifact
`published/current/fpf-index/snapshot.json`. Explicit bridges out of the context:
(1) upstream `ailev/FPF` — read-only source; (2) GitHub account billing/LFS quota —
Stas-owned; (3) MCP registry + external community — Stas-owned outward surface.
"Budget" in this packet always means the GitHub LFS quota (account layer), never
repo config — the term does not cross the boundary silently.

## Claim register (A.6 — atomic claims, one owner layer each)

| ID | Atomic claim | Owner layer | Acceptance check |
| --- | --- | --- | --- |
| CR-1 | The snapshot leaves git tracking; every job that consumed it regenerates it in-run, preserving `builtAt` pinned to the upstream commit time (#247 byte-stability guard) and the upstream blame enrichment (7,291 `lastCommittedAt` entries) | repo code + CI workflows | PR CI green with zero LFS fetches; regenerated bytes pass `validate:published` |
| CR-2 | `driftHours` measures the age of the *publication* (`hosted.publication.upstreamDate`), not the age of the newest upstream commit | src + tests | regression test: publication 15 d old vs upstream HEAD 1 h old ⇒ `breached === true` (fails on current main [fact], must pass after) |
| CR-3 | Failing sync / content-quality runs upsert one deduped issue and auto-close it on the next green run; the sync monitor escalates instead of re-dispatching after 2 failed recoveries for the same `upstream_ref` | CI workflows | issue open/close block functionally matches the proven `vercel-spend-monitor.yml` implementation (issue #249 open+auto-close, 2026-07-23 [fact]) |
| CR-4 | Production serves upstream `4b75b56c` or newer; A.7.1, A.6.RCD, C.19.2, A.15.PROD return 200 on fpf.sh | run-stance (maintainer dispatch after CR-1 merges) | live `curl /api/fpf/status` + four HTTP 200s, dated |
| CR-5 | A tracked `ROADMAP.md` (ranked next-5 + deferred) exists at repo root; CLAUDE.md:48-49 repoints to it; the Claude Code plugin is discoverable (README install block + hosted setup copy) | repo meta | files on `main`; `grep -ci plugin README.md` ≥ 1 (currently 0 [fact]) |
| CR-6 | LICENSE choice (code vs mirrored spec text scoped separately); MCP-registry publication go/no-go; LFS billing-page inspection | **Stas (board)** | explicit decision recorded; not executable by the maintainer session |

## Roles (A.15)

| Role | Holder | What it does |
| --- | --- | --- |
| Maintainer / CEO | this Claude Code session | decides, merges, dispatches sync, evidences CR-4 |
| Implementation PR agent (×3) | workflow subagents, worktree-isolated | one bounded PR each (CR-1+CR-3, CR-2, CR-5); capability fit checked at spec stage *before* dispatch (work-admission, A.15) |
| Adversarial reviewer | workflow subagents | attempt to refute each pushed diff against the two CR-1 caveats and test semantics |
| Merge gate | `ci.yml` | the only gate — no branch protection or rulesets on `main` [fact] |
| Board | Stas | CR-6 only |

One holder (this session) carries maintainer + dispatcher — stated explicitly (A.15).

## Method (F.11 quartet)

- **Method**: gated-delivery recovery — bounded PRs → CI → merge → dispatch → live verify.
- **MethodDescription** (design stance): this packet + the workflow script.
- **Work / run** (run stance): the actual PRs, runs, and curls, dated as they occur.
- **Signal**: `/api/fpf/status` bytes, `gh run` conclusions, test output.

Design-stance sentences never claim run-stance completion: no CR is "done" until its
acceptance check has dated evidence.

## Work

- Performed [fact]: 2026-07-28 six-dimension audit (42 agents, adversarially verified);
  drift expression re-read at `src/build/sync-monitor.ts:164`; LFS quota 403 re-verified.
- Next: execute CR-1 … CR-5. **One next owner: this maintainer session.**

## Evidence

- **Acceptance check**: `/api/fpf/status` reports `upstreamRef` ∈ {`4b75b56c`, newer}
  AND the four previously-404 pattern IDs return 200.
- **Falsifiable test**: the CR-2 regression case — it demonstrably fails against
  current `main` and passes after the fix.

## Term sheet (F.17)

| Term | Kind | Context | Meaning |
| --- | --- | --- | --- |
| fresh | status | this packet | publication ref == upstream HEAD within drift SLO |
| stale | status | this packet | publication ref behind upstream HEAD beyond SLO |
| breached | status | sync-monitor | `driftBreached \|\| !runtimeFresh` per CR-2 semantics |
| pending_sync | status | sync-monitor | upstream ahead, drift within SLO |
| DONE (per CR) | status | this packet | acceptance check has dated run-stance evidence |
